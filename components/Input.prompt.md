Text field with quiet uppercase label; use in forms, lead-capture, and editor panels.

```jsx
<Input label="Email" type="email" placeholder="you@studio.com" />
<Input label="Слово в директ" hint="Напишіть «АУДИТ»" />
```

Focus turns the hairline border sage. Pair with `Button` for forms.
